@model IEnumerable<OnlineShopWeb.Models.Product>
@{
    ViewBag.Title = "Kết quả tìm kiếm";
}

<h2>Kết quả tìm kiếm</h2>
<p>Từ khóa tìm kiếm: <strong>@ViewBag.Query</strong></p>

@if (Model != null && Model.Any())
{
    <div>
        @foreach (var product in Model)
        {
            <div class="col-sm-3" bis_skin_checked="1">
                <div class="product-image-wrapper product-hover" bis_skin_checked="1">
                    <div class="single-products" bis_skin_checked="1">
                        <div class="productinfo text-center" bis_skin_checked="1">
                            <img src="~/Content/images/home/product1.jpg" alt="">
                            <h2>@product.Price</h2>
                            <p>@product.Name</p>
                            <a href="#" class="btn btn-default add-to-cart"><i class="fa fa-shopping-cart"></i>Add to cart</a>
                        </div>
                    </div>
                    @*<div class="choose" bis_skin_checked="1">
                        <ul class="nav nav-pills nav-justified">
                            <li><a href="#"><i class="fa fa-plus-square"></i>Add to wishlist</a></li>
                            <li><a href="#"><i class="fa fa-plus-square"></i>Add to compare</a></li>
                        </ul>
                    </div>*@
                </div>
            </div>
        }
    </div>
}
else
{
    <p>Không tìm thấy sản phẩm nào!</p>
}
